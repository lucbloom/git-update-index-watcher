A tray icon that watches some of my git folders and runs git update-index --refresh on it to remove unchanged files with changed file changed dates
- Configurable list of git folders
- Right click menu
- Pause / unpause
